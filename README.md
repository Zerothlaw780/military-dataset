# military-dataset

Dataset workspace with source datasets, merged outputs, scripts, reports, and training artifacts.

## Structure

- `datasets/`: raw source datasets
- `merged_dataset/`: combined/normalized dataset output
- `scripts/`: dataset processing utilities
- `reports/`: analysis outputs and notes
- `training/`: training configs, runs, and checkpoints

